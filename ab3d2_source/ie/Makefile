# IE-native build entry point. The upstream Makefile supplies toolchain defaults.
IE_SOURCE_ROOT := $(abspath $(dir $(lastword $(MAKEFILE_LIST)))/..)

include $(IE_SOURCE_ROOT)/Makefile
include $(IE_SOURCE_ROOT)/ie/build.mk
